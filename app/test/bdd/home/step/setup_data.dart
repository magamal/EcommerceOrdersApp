import 'package:flutter_test/flutter_test.dart';

import '../../common/common_setup_data.dart';

/// Usage: Setup Data
Future<void> setupData(WidgetTester tester) async {
  await commonSetupData(tester);
}

import 'package:flutter_test/flutter_test.dart';

import '../../common/common_the_app_is_running.dart';

Future<void> theAppIsRunning(WidgetTester tester) async {
  await commonTheAppIsRunning(tester);
}
